name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  release:
    uses: krill-software/.github/.github/workflows/krill-app-release.yml@main
    with:
      product-name: "RSS Reader"
      example-file: "feeds.opml"
    secrets: inherit
    permissions:
      contents: write
